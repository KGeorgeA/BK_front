import { styled as muiStyled } from "@mui/material";
import MuiInput from '@mui/material/Input';
import styled from 'styled-components';

export const PriceInput = muiStyled(MuiInput)`
  width: 42px;
`